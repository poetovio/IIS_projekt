fetch:
  series: "price.dayahead"
  start_date: "2024-01-01"

preprocess:
  input_directory: "data/raw/electricity/prices"
  output_directory: "data/processed/electricity/prices"

train:
  window_size: 24
  train_ratio: 0.8
  epochs: 50
  batch_size: 64